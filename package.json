{
  "name": "clippy",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/mobile"
  ],
  "scripts": {
    "test": "vp test",
    "build": "vp build",
    "worker:dev": "npm --prefix apps/worker run dev",
    "worker:deploy": "npm run install:build && npm run dashboard:build && npm --prefix apps/worker run deploy",
    "install:build": "npm --prefix apps/install run build",
    "dashboard:build": "npm --prefix apps/dashboard run build",
    "dashboard:dev": "npm --prefix apps/dashboard run dev",
    "worker:migrate": "npm --prefix apps/worker run migrate",
    "worker:migrate:local": "npm --prefix apps/worker run migrate:local",
    "worker:secret": "npm --prefix apps/worker run secret",
    "ext:pack": "cd apps/extension && zip -r ../../clippy-extension.zip . -x '*.test.js' -x '*.test.ts' -x 'tests/*' -x 'key.pem' -x '.DS_Store' -x '*/.DS_Store' -x '.gitignore'",
    "ext:upload": "node scripts/upload-extension-zip.mjs",
    "mobile:start": "npm --prefix apps/mobile run start",
    "mobile:build:ios": "npm --prefix apps/mobile run ios:device",
    "bench:job": "node scripts/bench-job.mjs",
    "test:py": "cd apps/worker/container && python3 test_download.py && python3 test_server.py"
  },
  "dependencies": {
    "@clippy/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
