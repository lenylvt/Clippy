{
  "name": "clippy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp test",
    "worker:dev": "wrangler dev --config worker/wrangler.jsonc",
    "worker:deploy": "wrangler deploy --config worker/wrangler.jsonc",
    "worker:migrate": "wrangler d1 migrations apply clippy --local --config worker/wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.69.0"
  }
}
